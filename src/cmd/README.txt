Here lies all of the usable commands

For each command, create a directory, and a file that matches the exact name of the dir
declare a function in that directory with the exact same name
that will be the entrypoint